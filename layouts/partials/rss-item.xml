<item>
  <title>{{ .Title }}</title>
  <link>{{ .Permalink }}</link>
  <pubDate>{{ .Date.Format "2006-01-02" | safeHTML }}</pubDate>
  {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
  <guid>{{ .Permalink }}</guid>
  <description>{{ .Summary | html }}</description>
  <details>
  {{ with (index .Site.Data.post .File.BaseFileName) }}
      {{ with .imdb }}
          {{ .title }}
          ({{ if .countries }}{{ delimit .countries ", " }}{{ else }}Unknwown{{ end }}, {{ .year }}).
          {{ if .directors }} Dirigido por {{ delimit .directors ", " }}.{{ end }}
          {{ if .writers }} Escrito por {{ delimit .writers ", " }}.{{ end }}
          {{ if .cast }} Com {{ delimit .cast ", " }}.{{ end }}
      {{ end }}
  {{ end }}
  </details>
</item>
