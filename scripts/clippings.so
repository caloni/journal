call matchadd("search", "conseguiu, também, e")
call matchadd("search", "seu final adstringente")
call matchadd("search", "um rótulo da Austrália")
call matchadd("search", "descobri esses dias.")
call matchadd("search", "one of the advantages of designing without pencil and paper is that you are almost forced to avoid all avoidable complexities.")
call matchadd("search", "estética das formas, cores e efeitos que trabalham juntos para reforçar a própria estética.")
call matchadd("search", "mas de tudo, mas no caso")
call matchadd("search", "Agartha")
call matchadd("search", "há ainda mais qualidade")
