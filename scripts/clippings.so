call matchadd("search", "")
call matchadd("search", "de depois")
call matchadd("search", "Quando dois peões ganham de um quando")
call matchadd("search", "/>Game may have continued...}")
call matchadd("search", "{<br /><br />Game may have continued...}")
call matchadd("search", "aplicar o mate e forçar a dama a defender o mate deixando de proteger a torre.")
