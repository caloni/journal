call matchadd("search", "vamos por partes.")
call matchadd("search", "certo. nesse caso")
call matchadd("search", "Esclareceu de maneira impecável.")
call matchadd("search", "Ficou um pouco mais claro.")
call matchadd("search", "Muito interessante sua sugestão")
call matchadd("search", "Certo. Por enquanto é só.")
call matchadd("search", "Além disso os ingredientes")
call matchadd("search", "bem madura reduzida,")
call matchadd("search", "a mais. No final deu três tranças")
