call matchadd("search", "Filme feminista de época com gordinha branquela que parece criança")
call matchadd("search", "Mac,")
call matchadd("search", "não conta uma grande história,")
call matchadd("search", "Nada Pinked Smith.")
call matchadd("search", "nada disso não teria peso")
call matchadd("search", "a direção de Michael Mann não nos deixasse sonhar com as possibilidades desses dois atores em construírem uma realidade impossível.")
call matchadd("search", "Você começava a fazer e tinha um contador de 90 minutos.")
call matchadd("search", "fazer duas infusão")
call matchadd("search", "virem simpáticas?")
call matchadd("search", "daquelas surpresa")
