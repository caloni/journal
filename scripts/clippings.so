call matchadd("search", "result;")
call matchadd("search", "conhecer. Talvez um dia conheça")
call matchadd("search", "aceitávemos")
call matchadd("search", "This is actually the reason why this algorithm is called Bubble Sort: in each pass-through, the highest unsorted value “bubbles” up to its correct position.")
