call matchadd("search", "Não existe sequer uma entrada em português")
call matchadd("search", "mas como custa processamento descobrir qual a próxima potência de 2 que é maior que N uma aproximação válida é usar 4*N.")
call matchadd("search", "da soma adiciona")
call matchadd("search", "como um mapas")
call matchadd("search", "navegar por uma array.")
call matchadd("search", "pois existem node ramos no nível")
call matchadd("search", "the Warm Up challenges")
call matchadd("search", "how segment tree work")
call matchadd("search", "In order to do that segment trees help,")
call matchadd("search", "theirs nodes contain")
call matchadd("search", "the sum of determined intervals")
call matchadd("search", "The complexity of this solution is O(n) I guess,")
call matchadd("search", "I am not counting the operations into the set.")
call matchadd("search", "dois livros anteriores, [Algorithm for Dummies]http://www.caloni.com.br/algorithms-for-dummies/ e [Cracking the code interview](http://www.caloni.com.br/cracking-the-coding-interview/ eram muito chatos")
call matchadd("search", "Because the key determines the placement of the value, we use this principle to make lookups a cinch.")
call matchadd("search", "One type of problem in which recursion is a natural fit is when we need to delve into multiple layers of a problem without knowing how many layers there are.")
