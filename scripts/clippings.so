call matchadd("search", "encontro este Easter Egg (ainda fazem isso hoje em dia?) encontro esta pérola")
call matchadd("search", "estorno,")
call matchadd("search", "o restaurante que virou fast food.")
call matchadd("search", "desde o começo.")
call matchadd("search", "que já conversei")
call matchadd("search", "isso envolvia")
call matchadd("search", "quando você gosta do que faz, todo")
call matchadd("search", "Estas são ótimas memórias da época.")
call matchadd("search", "motivo pelo qual")
call matchadd("search", "Pesquisa para você ver")
