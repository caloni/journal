call matchadd("search", "que seja")
call matchadd("search", "um \"grande criador\" Xel'Naga.")
call matchadd("search", "esses patches continuam")
call matchadd("search", "de estão")
call matchadd("search", "/>Game may have continued...}")
