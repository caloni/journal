call matchadd("search", "que pode ser o motivo.")
call matchadd("search", "Devo testar passar")
call matchadd("search", "Com os fines")
call matchadd("search", "na versão prensa,")
call matchadd("search", "estava muito ocupada")
