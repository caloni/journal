call matchadd("search", "já sabermos de antemão todo o desenrolar da história.")
call matchadd("search", "Pronto, nossa árvore está pronta.")
call matchadd("search", "definir o valor 42 para o node 666:")
call matchadd("search", "os dois livros anteriores,")
call matchadd("search", "a mesma quantidade de água e farinha com o fermento")
call matchadd("search", "Toda a farinha usada")
