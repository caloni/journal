first know how the thing work
then know how the thing does not work

call matchadd("search", "Do Bit para o Código")
call matchadd("search", "Callback Hell")
call matchadd("search", "Winmock")
call matchadd("search", "Hoje em dia o caminho mais fácil é pelo Visual Studio Community, que instala por padrão um Windows SDK. Nessa instalação é possível modificar os itens checando o \"Debugging Tools for Windows\", que é o pacote que contém o ecossistema do WinDbg. Pulei essa parte. Tempo curto e me enrolei um pouco. E não era o caso de ficar focado na rotina de programador.")
call matchadd("search", "que é a melhor ever.")
call matchadd("search", "assembly, papel e caneta. E imaginação.")
call matchadd("search", "Sim. Como o Mercês me ajudou a lembrar,")
call matchadd("search", "Minha Palestra Sobre Windbg")
call matchadd("search", "Acredito que o exemplo desse post antigo possa exemplificar melhor o que quero dizer.")
call matchadd("search", "Cast Operator")
call matchadd("search", "com o cara do suporte, um sujeito bonachão")
call matchadd("search", "o barato fica loko,")
call matchadd("search", "O Bug Mais Bizarro que já Resolvi")
call matchadd("search", "o sincronismo e o encoding veio")
call matchadd("search", "Análise de DNA")
call matchadd("search", "Batalha")
call matchadd("search", "Awk")
call matchadd("search", "Leak de Memória")
call matchadd("search", "um programinha em Java (esqueci o nome),")
call matchadd("search", "https://gist.github.com/Caloni/368a8ce88206c13aac6ca27a56975af2")
call matchadd("search", "Historical Price")
call matchadd("search", "https://gist.github.com/Caloni/dd1429495c835a27ece61523e939ec7f")
call matchadd("search", "Printf")
call matchadd("search", "Find Path ou Por Que O Vcpkg Não Colocou o Path da Minha Biblioteca?")
call matchadd("search", "GetArgs v. Array")
call matchadd("search", "de tratava da")
call matchadd("search", "em um range pequeno possibilidades.")
call matchadd("search", "Lessons From Online Poker Exploit")
call matchadd("search", "Então vou gravar o vídeo,")
call matchadd("search", "Como Configurar Debug de Kernel Pela Rede (Hyper-V Edition)")
call matchadd("search", "Como Ordenar Três Números em Uma Entrevista")
call matchadd("search", "um pouco do que é possível fazer para acelerar um pouco")
call matchadd("search", "dia-a-dia,")
call matchadd("search", "ele não é muito popular nos scripts deste projeto.")
call matchadd("search", "as [diferentes checagens]")
call matchadd("search", "sendo inventada para adicionar mais formas de expressar condições.")
call matchadd("search", "Meditação é self debugging")
call matchadd("search", "AWK and DNA")
call matchadd("search", "https://youtu.be/wSfRFIUwB1o")
call matchadd("search", "Gerenciamento de Memória em C/C")
call matchadd("search", "Veja o vídeo desse post.")
call matchadd("search", "RIP Basílio Miranda")
call matchadd("search", "Passagem de Parâmetros C vs C+")
call matchadd("search", "Como descobrir se uma string é mutuamente rotativa")
call matchadd("search", "funcionamento, e também porque seu funcionamento")
call matchadd("search", "lista. Para")
call matchadd("search", "os elementos de uma lista completamente desordenada se comporta")
call matchadd("search", "Como funciona o bubble sort")
call matchadd("search", "Como funciona o insertion sort")
call matchadd("search", "Como inverter uma lista ligada")
call matchadd("search", "Como inverter uma string")
call matchadd("search", "Como pegar caracteres repetidos em uma string")
call matchadd("search", "Onde fica o meio de uma lista ligada?")
call matchadd("search", "Segundo maior número")
call matchadd("search", "Some o dígito com o novo número invertido; Multiplique o resultado por 10;")
call matchadd("search", "Como inverter um número")
call matchadd("search", "existir matemáticos")
call matchadd("search", "Como verificar se um número é primo")
call matchadd("search", "Código para Fibonacci")
call matchadd("search", "To solve the counting valleys problem")
call matchadd("search", "the cloud jump problem")
call matchadd("search", "repeated string problem")
call matchadd("search", "sales by match problem")
call matchadd("search", "Hacker Rank Warm Up")
call matchadd("search", "https://youtu.be/Ic7OO3Uw6J0")
call matchadd("search", "Árvore de segmentos")
call matchadd("search", "Hash Table Giratória")
call matchadd("search", "Edsger Dijkstra")
call matchadd("search", "Edsger Dijkstra Without Pencil and Paper")
call matchadd("search", "encontro encontro")
call matchadd("search", "esta pérola")
call matchadd("search", "história do chefe de cozinha que virou administrador de fast food")
call matchadd("search", "Easter Egg")
call matchadd("search", "Asciidoc is the best")
call matchadd("search", "Baixei [o projeto]")
call matchadd("search", "Zodiac, que é um static site generator escrito em awk. Mas claro! Gerar sites estaticamente é sobre manipular texto de entrada para texto de saída. Como não pensei nisso antes?")
call matchadd("search", "I realize that asciidoc is the best text standard for the future of the blog.")
call matchadd("search", "to develop a tool to replace hugo and automatize blog, ebooks and whatever the blog can be turned on (with any set of content).")
call matchadd("search", "The final solution for the blog is a single folder with a bunch of asciidoc files and its metadata. The publishing tool must be able to filter and generate related content based on metadata and parameters.")
call matchadd("search", "uma ideia ainda melhor que usar a awklib integrada com uma solução C++: o publicador de conteúdo ser um plugin do gawk.")
call matchadd("search", "o dia a dia de um blogger é muito mais fácil através de um `journal.txt` do que ficar se enveredando nas referências que o sistema de publicação te obriga a usar.")
call matchadd("search", "Porque um escritor não deve se limitar a um único formato. Seu único objetivo na vida é escrever. Seus metadados e o formato do seu texto irão definir para onde vai o conteúdo, mas ele não precisa se preocupar com isso.")
call matchadd("search", "Boas-vindas ao canal de Reversing do Telegrama")
call matchadd("search", "Descobrir ativação de classes WinRT")
call matchadd("search", "não está dando conta dos meus mais de 4000 posts. O maior peso dessa conversão é o file system.")
call matchadd("search", "duas estratégias de otimização:")
call matchadd("search", "Usar um único arquivo de leitura.")
call matchadd("search", "Usar um arquivo por mês na escrita.")
call matchadd("search", "A tradução do site para awk levou no total uns dois dias.")
call matchadd("search", "necessáriado")
call matchadd("search", "Finalmente blogawk")
call matchadd("search", "Lenght;")
call matchadd("search", "uma olhada no ótimo artigo de Raymond Chen")
call matchadd("search", "Explorando a HSTRING")
call matchadd("search", "estou cagando para a tradução. Use o inglês, estúpido.")
call matchadd("search", "Quando mais complexo")
call matchadd("search", "TRAFFIC")
call matchadd("search", "UWP apps não funcionam com proxy de loopback (resolvido)")
call matchadd("search", "Merge de backups")
call matchadd("search", "Corrigindo EPUB encoding")
call matchadd("search", "Tail recursion in loop while")
call matchadd("search", "Alocação sem construção")
call matchadd("search", "enquanto Size gera um código 100% correto Size não,")
call matchadd("search", "Se não instanciou o template não tem nada de errado")
call matchadd("search", "seria ser")
call matchadd("search", "com o a biblioteca Moq")
call matchadd("search", "Mock de Timer em Moq")
call matchadd("search", "Dividindo o assembly para conquistar")
call matchadd("search", "👏.")
call matchadd("search", "The AWK Programming Language (Alfred Aho)")

Do Bit para o Código
Callback Hell
Winmock
Hoje em dia o caminho mais fácil é pelo Visual Studio Community, que instala por padrão um Windows SDK. Nessa instalação é possível modificar os itens checando o "Debugging Tools for Windows", que é o pacote que contém o ecossistema do WinDbg. Pulei essa parte. Tempo curto e me enrolei um pouco. E não era o caso de ficar focado na rotina de programador.
que é a melhor ever.
assembly, papel e caneta. E imaginação.
Sim. Como o Mercês me ajudou a lembrar,
Minha Palestra Sobre Windbg
Acredito que o exemplo desse post antigo possa exemplificar melhor o que quero dizer.
Cast Operator
com o cara do suporte, um sujeito bonachão
o barato fica loko,
O Bug Mais Bizarro que já Resolvi
o sincronismo e o encoding veio
Análise de DNA
Batalha
Awk
Leak de Memória
um programinha em Java (esqueci o nome),
https://gist.github.com/Caloni/368a8ce88206c13aac6ca27a56975af2
Historical Price
https://gist.github.com/Caloni/dd1429495c835a27ece61523e939ec7f
Printf
Find Path ou Por Que O Vcpkg Não Colocou o Path da Minha Biblioteca?
GetArgs v. Array
de tratava da
em um range pequeno possibilidades.
Lessons From Online Poker Exploit
Então vou gravar o vídeo,
Como Configurar Debug de Kernel Pela Rede (Hyper-V Edition)
Como Ordenar Três Números em Uma Entrevista
um pouco do que é possível fazer para acelerar um pouco
dia-a-dia,
ele não é muito popular nos scripts deste projeto.
as [diferentes checagens]
sendo inventada para adicionar mais formas de expressar condições.
Meditação é self debugging
AWK and DNA
https://youtu.be/wSfRFIUwB1o
Gerenciamento de Memória em C/C
Veja o vídeo desse post.
RIP Basílio Miranda
Passagem de Parâmetros C vs C+
Como descobrir se uma string é mutuamente rotativa
funcionamento, e também porque seu funcionamento
lista. Para
os elementos de uma lista completamente desordenada se comporta
Como funciona o bubble sort
Como funciona o insertion sort
Como inverter uma lista ligada
Como inverter uma string
Como pegar caracteres repetidos em uma string
Onde fica o meio de uma lista ligada?
Segundo maior número
Some o dígito com o novo número invertido; Multiplique o resultado por 10;
Como inverter um número
existir matemáticos
Como verificar se um número é primo
Código para Fibonacci
To solve the counting valleys problem
the cloud jump problem
repeated string problem
sales by match problem
Hacker Rank Warm Up
https://youtu.be/Ic7OO3Uw6J0
Árvore de segmentos
Hash Table Giratória
Edsger Dijkstra
Edsger Dijkstra Without Pencil and Paper
encontro encontro
esta pérola
história do chefe de cozinha que virou administrador de fast food
Easter Egg
Asciidoc is the best
Baixei [o projeto]
Zodiac, que é um static site generator escrito em awk. Mas claro! Gerar sites estaticamente é sobre manipular texto de entrada para texto de saída. Como não pensei nisso antes?
I realize that asciidoc is the best text standard for the future of the blog.
to develop a tool to replace hugo and automatize blog, ebooks and whatever the blog can be turned on (with any set of content).
The final solution for the blog is a single folder with a bunch of asciidoc files and its metadata. The publishing tool must be able to filter and generate related content based on metadata and parameters.
uma ideia ainda melhor que usar a awklib integrada com uma solução C++: o publicador de conteúdo ser um plugin do gawk.
o dia a dia de um blogger é muito mais fácil através de um `journal.txt` do que ficar se enveredando nas referências que o sistema de publicação te obriga a usar.
Porque um escritor não deve se limitar a um único formato. Seu único objetivo na vida é escrever. Seus metadados e o formato do seu texto irão definir para onde vai o conteúdo, mas ele não precisa se preocupar com isso.
Boas-vindas ao canal de Reversing do Telegrama
Descobrir ativação de classes WinRT
não está dando conta dos meus mais de 4000 posts. O maior peso dessa conversão é o file system.
duas estratégias de otimização:
Usar um único arquivo de leitura.
Usar um arquivo por mês na escrita.
A tradução do site para awk levou no total uns dois dias.
necessáriado
Finalmente blogawk
Lenght;
uma olhada no ótimo artigo de Raymond Chen
Explorando a HSTRING
estou cagando para a tradução. Use o inglês, estúpido.
Quando mais complexo
TRAFFIC
UWP apps não funcionam com proxy de loopback (resolvido)
Merge de backups
Corrigindo EPUB encoding
Tail recursion in loop while
Alocação sem construção
enquanto Size gera um código 100% correto Size não,
Se não instanciou o template não tem nada de errado
seria ser
com o a biblioteca Moq
Mock de Timer em Moq
Dividindo o assembly para conquistar
👏.
The AWK Programming Language (Alfred Aho)
